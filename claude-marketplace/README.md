# AgentWatch Claude Marketplace (Legacy)

Consumer onboarding no longer depends on local Claude plugin marketplace setup.

Use:

```sh
curl -fsSL https://agentwatch.app/install/claude | sh
```

This folder is kept only for legacy/dev plugin experiments.
