# AgentWatch Claude Plugin (Legacy)

Consumer onboarding now patches Claude hooks directly during installer run:

```sh
curl -fsSL https://agentwatch.app/install/claude | sh
```

This plugin is retained only for compatibility with old local setups.
